const generateBoard = function(whiteQueen, blackqueen){
    var board = createArray(size, def);
};

// Takes in the size of the array/number of nested arrays and the default contents of each nested array
// Returns a nested array
function createArray(size, def) {

}